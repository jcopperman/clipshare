// jest.config.js

module.exports = {
    preset: 'ts-jest',
    testEnvironment: 'node',
    // Use the following line to explicitly match the test file:
    testMatch: ['<rootDir>/tests/*.tests.ts'], 
  };